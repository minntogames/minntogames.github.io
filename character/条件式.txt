idはnumber型(未配列)

nameはstring型(複数は配列(String型))

name_enはstring型(複数は配列(String型))

name_Kanaはstring型(未配列)

descriptionはstring型(複数は配列(String型))

worldはstring型(未配列)

raceは配列(string型)(複数は配列内配列(string型))

fightingStyleは配列(string型)(複数のは配列内配列(string型))

attributeは配列(string型)(複数は配列内配列(string型))

heightはnumber型(未配列)

ageはstring型(未配列)

birthday: {

yearはstring型(未配列)

monthはnumber型(未配列)

dayはnumber型(未配列)

};

personalityはstring型(未配列)

groupは配列(string型)(未配列)

imgは配列(string型)(複数は配列(string型))

imgsizeはstring型(未配列)

imgThumbsizeは配列(string型)(複数は配列(string型))

imageZoomPositionはstring型(未配列)

imageThumbPositionは配列(string型)(複数は配列(string型))

imgsize_mobileはstring型(未配列)

imageZoomPosition_mobileはstring型(未配列)

※別スタイル...配列可能な変数が配列状態の時のこと。
※未配列...別スタイルがあっても絶対配列化しない。
※配列(string型)...[string, string]な状態。
※配列内配列(string型)...[[string, string],[string, string]]な状態(stringが一つであっても別スタイル時は配列(string型)とする)。
※配列内配列(string型)で中の配列(string型)が単体の場合[string]、[]から外す([[string]]ではなく[string])
※配列内配列(string型)で中の配列(string型)が単体の場合[string]で配列した場合、このように変換しなさい[[string],[string...]]

